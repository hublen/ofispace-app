import { SCREEN_WIDTH } from '../../../constants/dimensions';

export default {
  semiFullWidthView: {
    width: SCREEN_WIDTH * 0.45,
    paddingBottom: 2,
    flexDirection: 'column',
  },
  scrollPadding: {
    paddingLeft: 13,
  },
};
